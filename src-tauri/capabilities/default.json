{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No JavaScript bridge permissions are exposed to the FastAPI-served UI.",
  "windows": ["main"],
  "permissions": []
}
